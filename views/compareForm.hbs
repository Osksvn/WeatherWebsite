<section class="hero is-info is-medium is-bold">
    <div class="hero-head">
        <nav class="navbar">
            <div class="container">
                <div class="navbar-brand">
                    <a href="/" class="navbar-item">
                        <p class="title">Weather Website</p>
                    </a>
                    <span class="navbar-burger burger" data-target="navbarMenuHeroB">
                        <span></span>
                        <span></span>
                        <span></span>
                    </span>
                </div>
                <div id="navbarMenuHeroB" class="navbar-menu">
                    <div class="navbar-end">
                        <a href="/" class="navbar-item">
                            Home
                        </a>
                        <a class="navbar-item is-active" href="/compare">Compare weather</a>
                        <a class="navbar-item" href="/database">Database items</a>
                    </div>
                </div>
            </div>
        </nav>
    </div>
</section>

<section class="section">
    <form method="post" action="/comparison">
        <div class="container">
            {{#if city}}
            <div class="row columns">
                <div class="column">
                    <div class="control">
                        <div class="select">
                            <select>
                                <option id="cityone">Select city one</option>
                                {{#each city}}
                                <option id="cityone" name="cityone" value="{{id}}">{{name}}</option>
                                {{/each}}
                            </select>
                        </div>
                    </div>
                </div>
                <div class="column">
                    <div class="control">
                        <div class="select">
                            <select>
                                <option>Select city two</option>
                                {{#each city}}
                                <option id="citytwo" name="citytwo" value="{{id}}">{{name}}</option>
                                {{/each}}
                            </select>
                        </div>
                    </div>
                </div>
                <div class="column">
                    <div class="control">
                        <div class="select">
                            <select>
                                <option>Select city three</option>
                                {{#each city}}
                                <option  id="citythree" name="citythree" value="{{id}}">{{name}}</option>
                                {{/each}}
                            </select>
                        </div>
                    </div>
                </div>
                <div class="column">
                    <div class="control">
                        <button class="button is-primary">Compare</button>
                    </div>
                </div>
            </div>
        </div>
    </form>


    {{else}}
    <p>no city entries</p>
    {{/if}}

</section>
